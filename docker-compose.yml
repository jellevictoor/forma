services:
  fitness-coach:
    build: .
    container_name: fitness-coach
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    env_file:
      - .env
    stdin_open: true
    tty: true
    command: ["uv", "run", "serve"]
